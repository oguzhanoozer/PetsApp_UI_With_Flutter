import 'package:flutter/material.dart';

class BorderConstants {
  static const topLeftRight = BorderRadius.only(
    topLeft: Radius.circular(26),
    topRight: Radius.circular(26),
  );

  static final circularRadiusLow = BorderRadius.circular(22.0);
  static final circularRadiusNormal = BorderRadius.circular(26.0);
}
